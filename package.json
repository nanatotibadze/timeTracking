{
  "name": "time-tracking-dashboard-main",
  "version": "1.0.0",
  "description": "Your challenge is to build out this dashboard and get it looking as close to the design as possible.",
  "default": "index.html",
  "scripts": {
    "compile:sass": "sass src/sass/main.scss css/style.css -w",
    "start": "parcel index.html",
    "build": "parcel build index.html --dist-dir ./dist"
  },
  "author": "Nanuka Totibadze",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.8.2",
    "parcel": "^2.8.2",
    "sass": "^1.57.1"
  }
}
